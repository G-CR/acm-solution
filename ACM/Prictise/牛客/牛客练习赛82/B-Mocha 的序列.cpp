#include <bits/stdc++.h>
using namespace std;

int n, m, x, l, r, k;

int main() {
	scanf("%d %d", &n, &m);
	while(m--) {
		scanf("%d %d %d", &x, &l, &r);
		if(x == 2) scanf("%d", &k);
		
		if(x == 3) {
			puts("0");
		}
	}
}